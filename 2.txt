Prompt 2 — for fixing/separating Contact Us

The Contact page currently uses the same content and endpoint as "Start a Project" — separate them. Keep /contact as a lightweight, single-page form (not a wizard) for general inquiries only, using the existing copy: "Start a Conversation... Tell us about your project and we'll get back to you within 24 hours", with Email/Working Hours/Location/Response Time info panels, and the form fields (Full Name, Email, Phone, Project Type, Budget Range, Message) posting to /api/contact as it already does — this creates a "Contact Lead", separate from a full project request. Update all "Start a Project" buttons/links across the site (nav CTA, hero CTA, footer, portfolio/services page CTAs) to point to /start-project instead of /contact, so the two flows are visually and functionally distinct: Contact Us = quick question → Contact Leads in admin; Start a Project = detailed multi-step intake → Client Projects + Client Messages in admin.